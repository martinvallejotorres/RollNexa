import {useState,type FormEvent} from 'react';import {Navigate,Link} from 'react-router-dom';import {useAuth} from '../auth';
export default function AuthPage(){const {user,login,register}=useAuth();const [mode,setMode]=useState<'login'|'register'>('login');const [form,setForm]=useState({username:'',email:'',password:''});const [error,setError]=useState('');const [busy,setBusy]=useState(false);if(user)return <Navigate to="/" replace/>;async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{if(mode==='login')await login(form.username,form.password);else await register(form.username,form.email,form.password)}catch(e){setError(e instanceof Error?e.message:'No pudimos continuar')}finally{setBusy(false)}}return <main className="auth-page"><section className="auth-copy"><span className="eyebrow">TU PRÓXIMA HISTORIA EMPIEZA ACÁ</span><h1>Encontrá la mesa que estabas buscando.</h1><p>Campañas, gente y fechas en un solo lugar. Sin planillas eternas ni mensajes perdidos.</p><div className="feature-row"><span>◆ Grupos reales</span><span>◆ Calendario compartido</span><span>◆ Chat por campaña</span></div></section><section className="auth-card panel"><Link className="brand mobile-brand" to="/"><span className="brand-mark">R</span>Rollnexa</Link><div className="tabs"><button className={mode==='login'?'active':''} onClick={()=>setMode('login')}>Ingresar</button><button className={mode==='register'?'active':''} onClick={()=>setMode('register')}>Crear cuenta</button></div><form onSubmit={submit}><label>Usuario<input required autoComplete="username" minLength={3} maxLength={32} value={form.username} onChange={e=>setForm({...form,username:e.target.value})} placeholder="NightWizard"/></label>{mode==='register'&&<label>Email<input required type="email" autoComplete="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="vos@ejemplo.com"/></label>}<label>Contraseña<input required type="password" autoComplete={mode==='login'?'current-password':'new-password'} minLength={8} value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="Mínimo 8 caracteres"/></label>{error&&<div className="alert error">{error}</div>}<button className="button full" disabled={busy}>{busy?'Procesando…':mode==='login'?'Ingresar a Rollnexa':'Crear mi cuenta'}</button></form><p className="legal">Al continuar aceptás las normas de convivencia de la comunidad.</p></section></main>}

